#include<iostream>
using namespace std;

int main()
{
	float a, b, h;
	cout << "Enter the Base and Height of the Triangle: ";
	cin >> b >> h;

	a = (b * h) / 2;

		cout << "Area of the Triangle is: " << a ;

	return 0;
}